#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    setlocale(LC_ALL, "ukr");
/*    int a = 5;
    int pw = pow(a, 3);*/

    //double cb = cbrt(pww); кубічний ступінь
    /*double sq = pow(pw, 1.0 / 3);
    double pi = acos(-1);
    double deg = 30
        double rad = (deg * pi) / 180;
    double sn = sin(rad)*/

    //double lg = log(100);
    //cout << lg;

    int a = 5; ////-1 завдання
    int b = 3;
    double res = sqrt(pow(a, 2) + pow(b, 2));
    cout << res;

    //double ab = abs(-2); модуль
}